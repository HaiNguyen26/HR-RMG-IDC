{
  "name": "hr-management-frontend",
  "version": "1.0.0",
  "description": "HR Management System Frontend",
  "private": true,
  "homepage": "/hr",
  "dependencies": {
    "axios": "^1.6.2",
    "react": "^18.2.0",
    "react-datepicker": "^8.9.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "cross-env NODE_OPTIONS=--max-old-space-size=4096 react-scripts start",
    "dev:hr": "cross-env NODE_OPTIONS=--max-old-space-size=4096 PORT=3001 react-scripts start",
    "dev:employee": "cross-env NODE_OPTIONS=--max-old-space-size=4096 PORT=3002 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:3000",
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}